<!-- Admin Mobile Bottom Navigation -->
<nav class="admin-mobile-bottom-nav">
    <div class="nav-item" onclick="navigateTo('/admin/billing/dashboard')">
        <div class="nav-icon <%= currentPage === 'dashboard' ? 'active' : '' %>">
            <i class="bi bi-house"></i>
        </div>
        <span class="nav-label">Dashboard</span>
    </div>
    
    <div class="nav-item" onclick="navigateTo('/admin/billing/customers')">
        <div class="nav-icon <%= currentPage === 'customers' ? 'active' : '' %>">
            <i class="bi bi-person-fill"></i>
        </div>
        <span class="nav-label">Pelanggan</span>
    </div>
    
    <div class="nav-item" onclick="navigateTo('/admin/billing/invoices')">
        <div class="nav-icon <%= currentPage === 'invoices' ? 'active' : '' %>">
            <i class="bi bi-receipt"></i>
        </div>
        <span class="nav-label">Tagihan</span>
    </div>
    
    <div class="nav-item" onclick="navigateTo('/admin/billing/payments')">
        <div class="nav-icon <%= currentPage === 'payments' ? 'active' : '' %>">
            <i class="bi bi-credit-card-fill"></i>
        </div>
        <span class="nav-label">Pembayaran</span>
    </div>
    
    <div class="nav-item" onclick="navigateTo('/admin/billing/mapping')">
        <div class="nav-icon <%= currentPage === 'mapping' ? 'active' : '' %>">
            <i class="bi bi-map"></i>
        </div>
        <span class="nav-label">Peta</span>
    </div>
</nav>

<style>
.admin-mobile-bottom-nav {
    position: fixed;
    bottom: 0;
    left: 0;
    right: 0;
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    box-shadow: 0 -4px 20px rgba(0, 0, 0, 0.15);
    display: flex;
    justify-content: space-around;
    align-items: center;
    padding: 8px 0;
    z-index: 1000;
    border-top: 1px solid rgba(255, 255, 255, 0.1);
}

.admin-mobile-bottom-nav .nav-item {
    display: flex;
    flex-direction: column;
    align-items: center;
    cursor: pointer;
    padding: 4px 8px;
    border-radius: 8px;
    transition: all 0.3s ease;
    min-width: 60px;
}

.admin-mobile-bottom-nav .nav-item:hover {
    background: rgba(255, 255, 255, 0.1);
    transform: translateY(-2px);
}

.admin-mobile-bottom-nav .nav-icon {
    width: 32px;
    height: 32px;
    display: flex;
    align-items: center;
    justify-content: center;
    border-radius: 8px;
    margin-bottom: 4px;
    transition: all 0.3s ease;
    color: rgba(255, 255, 255, 0.7);
    font-size: 18px;
}

.admin-mobile-bottom-nav .nav-icon.active {
    background: rgba(255, 255, 255, 0.2);
    color: white;
    transform: scale(1.1);
    box-shadow: 0 2px 8px rgba(0, 0, 0, 0.2);
}

.admin-mobile-bottom-nav .nav-label {
    font-size: 10px;
    color: rgba(255, 255, 255, 0.8);
    font-weight: 500;
    text-align: center;
    line-height: 1.2;
}

.admin-mobile-bottom-nav .nav-item:hover .nav-icon {
    color: white;
    transform: scale(1.05);
}

.admin-mobile-bottom-nav .nav-item:hover .nav-label {
    color: white;
}

/* Desktop - hide mobile navbar */
@media (min-width: 769px) {
    .admin-mobile-bottom-nav {
        display: none;
    }
}

/* Mobile - show navbar and add bottom padding */
@media (max-width: 768px) {
    body {
        padding-bottom: 80px;
    }
    
    .admin-mobile-bottom-nav {
        display: flex;
    }
    
    /* Hide sidebar on mobile */
    .sidebar {
        transform: translateX(-100%);
    }
    
    .sidebar.show {
        transform: translateX(0);
    }
}

/* Ensure main content doesn't overlap with navbar */
@media (max-width: 768px) {
    .main-content {
        margin-bottom: 80px;
        margin-left: 0 !important;
        width: 100% !important;
    }
}
</style>


