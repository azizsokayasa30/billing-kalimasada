# Kalimasada SaaS — Nginx reverse proxy (auto-generated)
# Generated: 2026-06-09T06:15:17.487Z
# Jangan edit manual — gunakan Management Portal /management/reverse-proxy
# Subdomain tenant (4): sapuran, lebakwangi, login, default
# Proxy eksternal (1): bna.kalimasada-app.com

upstream km_proxy_bna {
    server 192.168.166.196:3003;
    keepalive 16;
}

upstream kalimasada_app {
    server 127.0.0.1:4555;
    keepalive 32;
}

upstream kalimasada_management {
    server 127.0.0.1:4556;
    keepalive 16;
}

server {
    listen 443 ssl http2;
    listen [::]:443 ssl http2;
    server_name kalimasada-app.com *.kalimasada-app.com sapuran.kalimasada-app.com lebakwangi.kalimasada-app.com login.kalimasada-app.com default.kalimasada-app.com;
    ssl_certificate /etc/letsencrypt/live/kalimasada-app.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/kalimasada-app.com/privkey.pem;
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_prefer_server_ciphers on;

    location ^~ /.well-known/acme-challenge/ {
        root /var/www/certbot;
        default_type "text/plain";
        try_files $uri =404;
    }

    location / {
        proxy_pass http://kalimasada_app;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_read_timeout 300s;
        proxy_connect_timeout 60s;
        proxy_send_timeout 300s;
        client_max_body_size 50m;
    }
}

server {
    listen 80;
    listen [::]:80;
    server_name kalimasada-app.com *.kalimasada-app.com sapuran.kalimasada-app.com lebakwangi.kalimasada-app.com login.kalimasada-app.com default.kalimasada-app.com;

    location ^~ /.well-known/acme-challenge/ {
        root /var/www/certbot;
        default_type "text/plain";
        try_files $uri =404;
    }

    location / {
        return 301 https://$host$request_uri;
    }
}
server {
    listen 443 ssl http2;
    listen [::]:443 ssl http2;
    server_name manage.kalimasada-app.com;
    ssl_certificate /etc/letsencrypt/live/kalimasada-app.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/kalimasada-app.com/privkey.pem;
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_prefer_server_ciphers on;

    location ^~ /.well-known/acme-challenge/ {
        root /var/www/certbot;
        default_type "text/plain";
        try_files $uri =404;
    }

    location / {
        proxy_pass http://kalimasada_management;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_read_timeout 300s;
        proxy_connect_timeout 60s;
        proxy_send_timeout 300s;
        client_max_body_size 50m;
    }
}

server {
    listen 80;
    listen [::]:80;
    server_name manage.kalimasada-app.com;

    location ^~ /.well-known/acme-challenge/ {
        root /var/www/certbot;
        default_type "text/plain";
        try_files $uri =404;
    }

    location / {
        return 301 https://$host$request_uri;
    }
}

# Routing:
#   manage.kalimasada-app.com  → Management Portal (PM2 kalimasada-saas-management 127.0.0.1:4556)
#   sapuran.kalimasada-app.com  → Tenant: sapuran (lokal 127.0.0.1:4555)
#   lebakwangi.kalimasada-app.com  → Tenant: lebakwangi (lokal 127.0.0.1:4555)
#   login.kalimasada-app.com  → Tenant: login (lokal 127.0.0.1:4555)
#   default.kalimasada-app.com  → Tenant: default (lokal 127.0.0.1:4555)
#   bna.kalimasada-app.com  → Eksternal 192.168.166.196:3003
#   *.kalimasada-app.com         → Wildcard (tenant baru otomatis)
#   kalimasada-app.com           → Apex domain
#   http://192.168.166.197/     → Akses LAN (IP langsung)
#   http://192.168.166.197:4555/  → Node langsung (tanpa nginx)

# Proxy kustom — arahkan domain ke server/backend lain
server {
    listen 443 ssl http2;
    listen [::]:443 ssl http2;
    server_name bna.kalimasada-app.com;
    ssl_certificate /etc/letsencrypt/live/kalimasada-app.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/kalimasada-app.com/privkey.pem;
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_prefer_server_ciphers on;

    location ^~ /.well-known/acme-challenge/ {
        root /var/www/certbot;
        default_type "text/plain";
        try_files $uri =404;
    }

    location / {
        proxy_pass http://km_proxy_bna;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_read_timeout 300s;
        proxy_connect_timeout 60s;
        proxy_send_timeout 300s;
        client_max_body_size 50m;
    }
}

server {
    listen 80;
    listen [::]:80;
    server_name bna.kalimasada-app.com;

    location ^~ /.well-known/acme-challenge/ {
        root /var/www/certbot;
        default_type "text/plain";
        try_files $uri =404;
    }

    location / {
        return 301 https://$host$request_uri;
    }
}

#   bna.kalimasada-app.com → 192.168.166.196:3003 (Billing SKY-BNA)

# Akses LAN / IP langsung (HTTP port 80, tanpa domain)
server {
    listen 80 default_server;
    listen [::]:80 default_server;
    server_name 192.168.166.197 _;

    location ^~ /.well-known/acme-challenge/ {
        root /var/www/certbot;
        default_type "text/plain";
        try_files $uri =404;
    }

    location / {
        proxy_pass http://kalimasada_app;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_read_timeout 300s;
        proxy_connect_timeout 60s;
        proxy_send_timeout 300s;
        client_max_body_size 50m;
    }
}
